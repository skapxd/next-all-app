// @ts-check
import fs from "fs";
/**
 * @param {Object} props
 * @param {string} props.dataUrl
 * @param {string} props.fileName
 */
export const dataUrlToFile = (props) => {
  const { dataUrl, fileName } = props;

//   fs.write()
};
